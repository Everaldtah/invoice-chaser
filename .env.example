# Database
DB_PATH=./data/invoices.db

# SMTP (required for sending emails)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=SG.xxxxxx

# Optional: Payment link base URL (appended with /pay/:invoiceId)
PAYMENT_BASE_URL=https://yourapp.com

# Server
PORT=3000
